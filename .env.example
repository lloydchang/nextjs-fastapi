# .env.example
# ==========================
# Environment Variable Example Configuration
# ==========================

# Google API Key for authenticating requests to the Google gemini-1.5-flash model.
# Obtain this key from your Google Cloud Console.
NEXT_PUBLIC_GOOGLE_API_KEY=your_google_api_key_here

# Base URL for Google AI API (Optional)
# Modify this if the API base URL changes or if using a different region.
NEXT_PUBLIC_GOOGLE_API_BASE_URL=https://api.google.com/v1

# ==========================
# Instructions:
# 1. Copy this file as `.env.local` in your project root.
# 2. Replace 'your_google_api_key_here' with the actual API key.
# 3. Add the `.env.local` file to `.gitignore` to protect your API key.
